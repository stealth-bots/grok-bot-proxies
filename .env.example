# Copy to .env.local for `vercel dev`. Never commit .env.local.
# Shape only — replace with your own values.

# Slack hop (/slack)
CURSOR_WEBHOOK_URL=https://api2.cursor.sh/automations/webhook/<uuid>
CURSOR_WEBHOOK_KEY=crsr_...
# SLACK_SIGNING_SECRET=

# Linear hop (/linear) — different Cursor routine from Slack
LINEAR_CURSOR_WEBHOOK_URL=https://api2.cursor.sh/automations/webhook/<uuid>
LINEAR_CURSOR_WEBHOOK_KEY=crsr_...
# Required. Without it, POST /linear returns 503 and does not forward.
LINEAR_WEBHOOK_SECRET=

# Agent session ack. Linear marks a session unresponsive without an activity within
# 10s, so /linear posts a `thought` before handing off to Cursor. These mint an
# app actor token via client_credentials — enable that grant on the Linear app first.
# Without them the ack is skipped and the Cursor forward still runs.
LINEAR_CLIENT_ID=
LINEAR_CLIENT_SECRET=

# Shared bearer Grok Bot sends to /linear/activity and /linear/comment. Required for
# both; without it they return 503 rather than letting anyone write as the agent.
LINEAR_ACTIVITY_SECRET=

# Request bodies are NOT logged by default — they carry issue and comment text, and
# logs are retained. Set to 1 to opt in while debugging, then turn it back off.
# Token-shaped strings are redacted either way.
# LOG_PAYLOADS=1
