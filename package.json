{
	"name": "slack-events-proxy",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"deploy": "npx vercel --prod",
		"dev": "npx vercel dev --listen 43123",
		"start": "npx vercel dev --listen 43123",
		"test": "vitest run",
		"wrangler:dev": "wrangler dev --ip 127.0.0.1 --port 43123",
		"wrangler:deploy": "wrangler deploy",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@types/node": "^22.15.0",
		"typescript": "^5.5.2",
		"vitest": "~4.1.0",
		"wrangler": "^4"
	}
}
